{
  "config": {
    "step": {
      "user": {
        "title": "Github Config Sync"
      }
    },
    "abort": {
      "addon_only": "This is a Home Assistant add-on, not a HACS integration. Install it from Settings → Add-ons → Add-on Store."
    }
  }
}